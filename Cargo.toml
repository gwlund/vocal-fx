[workspace]
resolver = "2"
members = [
    "plugins/gain",
    "plugins/gate",
    "plugins/eq",
    "plugins/compressor",
    "plugins/delay",
    "plugins/reverb",
    "common",
    "xtask",
]

[profile.release]
lto = "thin"
strip = "symbols"
